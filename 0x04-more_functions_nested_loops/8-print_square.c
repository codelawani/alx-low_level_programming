#include "main.h"
/**
* print_square - Prints a square
* @size: first integer
* Return: 0
*/
void print_square(int size)
{
	int x, y;

	if (n > 0)
	{
	for (x = 0 ; x <= size ; x++)
	{
	for (y = 0 ; y <= size ; y++)
	{
		_putchar('#');
	}
		_putchar('\n');
	}
	}
	if (x == 0)
	{
		_putchar('\n')
	}
}
